# P6 — Post-Freeze Change Reconciliation
**Gate 4.1 · v4.2.5-closure · 2026-09-18 · Directive P6.2/P6.3: حصر التعديلات المعلنة وإثبات النطاق**

## الحسم (الحالة الأولى — التعديلات كانت مضمنة بالفعل)

| الملف المعلن | الحقيقة القابلة للفحص | التصنيف |
|---|---|---|
| `release/v4.2.5-closure/engine-semantics-contract.md` | أنشئ في `9ad7843db1668558aae7ff0c1fa1c86abcd2da8e`، **مضمن في `c896cec`** (git cat-file مؤكد)؛ توثيقي؛ داخل نطاق الـ45/45 | لا يغيّر السلوك؛ لا يوسّع المانيفست؛ ضمن مراجعة C8 |
| `release/v4.2.5-closure/external-evidence-model.md` | أنشئ في `9ad7843…`، **مضمن في `c896cec`**؛ توثيقي؛ داخل نطاق الـ45/45 | لا يغيّر السلوك؛ لا يوسّع المانيفست؛ ضمن مراجعة C8 |
| `tmp-freeze-4.2.5.ps1` | **لم يُتتبَّع قط** (git ls-files = 0) و**محذوف من القرص بعد الاستخدام**؛ أداة بناء تُتحقق مخرجاتها مستقلًا (45/45 من أرشيف GitHub بلا أي اعتماد على الأداة) | لا أثر له على قابلية إعادة إنتاج السجل |

الظهور في "سجل التنفيذ" كان **إخراجًا للعملية** لا تغييرًا لاحقًا للملفات.

## العيب الحقيقي الذي كشفه CI (والفشل الأربعة الموثقة)
اختبار `encoding-hygiene` (مضمن في 696 اختبار المشروع) رفض **تسلسلات mojibake
CP1252** في حقل `decision` لسجلي التجميد 4.2.4/4.2.5: شرطة `—` كُتبت عبر
PowerShell 5.1 (قراءة ANSI لسكربتات بلا BOM) فتضاعف ترميزها. الأكشنز الفاشلة:
`35324679195` (3a7ca37) · `35324933315` (83ac866) · `35326319740` (9ad7843) ·
`35326547879` (c896cec) — البداية من أول commit أدخل سجلًا مكتوبًا بـPowerShell.

## المعالجة
- فك الترميز العكسي (طبقة/طبقتين بحسب السجل) بفحص CI-style بعد كل طبقة + إبقاء JSON قابلًا للتحليل → **صفر أنماط mojibake، حقلان مستعادان حرفيًا**.
- إعادة التقاط هاش الملفين داخل `freeze-record-4.2.5.json` (بلا BOM) → **45/45 ALL-MATCH على القرص والنسختين**.
- التحقق المحلي الكامل: **encoding-hygiene 3/3 PASS · npm test 696/696 PASS**.

## أثر ذلك على النزاهة (مفهوم)
تعديل أي ملف بعد تجميده يبطل هاشه — لذلك أُعيد التقاط الهاشات **داخل السجل نفسه**
بعد الإصلاح وأُعيد التحقق 45/45 من القرص. السجل المعدل نفسه هو المرجع الجديد
الوحيد، وتوثيق هذا الاستبدال هنا جزء من السجل الموقَّع مستقبلًا. القرار unchanged:
**CONDITIONAL NO-GO — C7=PARTIAL · C8=PENDING · C9=PENDING**.
