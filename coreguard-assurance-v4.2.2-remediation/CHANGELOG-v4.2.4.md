# CHANGELOG — v4.2.4-final-freeze (Engine 2.1.2)
**Gate 4.1 · 2026-09-18 · Directive: دورة الإغلاق القصيرة المفوَّضة (P1–P5) بعد قرار NO-GO**

## لماذا دورة جديدة
المرحلة 3 من التوجيه طلبت فصل `PRECONDITION_FAILURE` عن `NOT_APPLICABLE` ومنع احتساب
أي مقدمة مفقودة نجاحًا. التدقيق أظهر أن الحارس الجزئي (v2.1.1) كان يمنع التقارب الشفوي
فقط، بينما ظلّت `Record` تقبل `NOT_APPLICABLE`/`ABSENT` كإثبات فشل مغلق — نفس عائلة
العيب في السيناريو 9، لكنها أعمق. أُصلحت جذريًا في **2.1.2**.

## تغييرات المحرك (2.1.1 → 2.1.2)
1. **دلالة صارمة لـ`Record`**: السيناريو الإلزامي لا يُحسب نجاحًا إلا بـ`FAIL`/`BLOCKED`
   قابلًا للملاحظة — `NOT_APPLICABLE` و`ABSENT` لم يعد أحدهما يجتاز توقع الفشل شكليًا.
2. **حالة `PRECONDITION_FAILURE` صريحة** لأي سيناريو تفقد مقدمته، وتُسقط المجموعة
   كاملة (`exit 1`) حتى لو بقي كل شيء آخر PASS.
3. **حذف السيناريو الشكلي 15** (`NEG-missing-drift-map`): كان يختبر إصدار
   `NOT_APPLICABLE` لخريطة غائبة — سلوك سليم موثق وليس فشلًا مغلقًا. نمط الفشل الحقيقي
   (خريطة متلاعب بها → FAIL) مغطى بالسيناريو 14. المجموعة الآن **16 سيناريو**.
4. توحيد سلسلة الإصدار في التعليق (كانت 2.1.0 في سطر 2 بينما المتغير 2.1.1).

## نتائج الدورة (النسخة الحالية، بيئة مؤلف التعديل)
- Self-test: **21/21 PASS**
- Negative suite: **16/16 fail-closed · preconditionFailures=0**
- التشغيل القياسي: **33/33 PASS · VERIFIED-READONLY-ONLY**
- Self-check: PASS (33 findings، صفر أخطاء) · Independent Node validator: **PASS (649 فحصًا)**

## الأرقام الموحدة (مطابق للتوجيه P2)
| الفئة | العدد |
|---|---|
| ملفات الإصدار الأساسية | **23** |
| ملفات العزل | **19** |
| إجمالي عناصر التجميد | **42** |
| مستثنى من الخريطة | **1** — سجل التجميد نفسه (مرجع ذاتي)؛ يُتحقق منه عبر مطابقة القرص مقابل أرشيف GitHub مباشرة |

## ما لم يتغير
- الأدلة المجمّدة السابقة (v4.2.3): **45/45 مطابقة** — لم تُمس ولا تُعاد كتابتها.
- القرار: `CONDITIONAL NO-GO` hardcoded — لا GO ذاتي من الأداة مهما كانت النتائج.
