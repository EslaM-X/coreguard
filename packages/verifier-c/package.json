{
  "name": "@coreguard/verifier-c",
  "version": "0.1.0",
  "description": "CGEP/1 Verifier C — stdlib-only Python re-derivation (Verifier B mirror). Transport/marshalling wrapper (ESM) around the Python bridge; spawns the interpreter, moves newline-delimited JSON, drives EIP-1271 witness read steps, and rejects post-execution evidence at the pre-execution seam (Q-W3-6/Q-FW10). Reproducibility metadata only, per Dec-C-9.",
  "type": "module",
  "private": true,
  "main": "./index.js",
  "exports": {
    ".": "./index.js"
  },
  "files": [
    "index.js",
    "bridge.py",
    "verifier_c/"
  ],
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
