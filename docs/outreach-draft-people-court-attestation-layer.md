# Outreach draft — People's Court / Epistemic Labs (not yet posted)

Status: **draft only — no message has been sent.** The thread currently ends
with our correction reply of 2026-09-23 18:15:37Z (`#18570936`) and we are
holding the posture: critique → correction → verification → reply. Posting
again before the counterparty responds to `1e75fba` risks a third consecutive
message from our side and can be read as an ask we explicitly said we would not
make.

## Why this is drafted now

Requested as the "strategic touch" step: thank them for the precision,
announce the schema is now the documented standard, and state openness — as a
verification/attestation layer — without any actual request.

## The constraint that must survive into any version

- Read-only review ≠ endorsement. Never phrase this as "validated by", "approved
  by", or "in partnership with" People's Court.
- They did not run the verifier and did not check the chain — do not imply they
  did.
- No funding / integration / pilot / adjudication asks. Nothing that reads as
  "what do you think?" after a closed exchange.

## Variant A — minimal (safe, likely unnecessary until they reply)

> [same thread, nested] Subsequent to 1e75fba the corrected label rules are now
> the documented, tested standard (docs/evidence-package-standard.md; one-command
> package CLI, 11/11 → EVP_OK). No action needed on your side — flagging only so
> the thread record stays current.

## Variant B — fuller (positioning; borderline, may read as an opening)

> [same thread, nested] Thanks again for the two label catches — the corrected
> rules are now a documented, one-command, fail-closed standard in the repo
> (docs/evidence-package-standard.md). If a future boundary, failure mode, or
> use case would benefit from an independent execution-attestation fixture
> shaped by these labels, we remain open to scoping it on your terms. No
> commitment assumed either way.

## Where this lives

Repos/channels: GitHub discussion (elizaOS #21788, our nested reply) — the only
sanctioned external channel so far; `request-demo` on peoplescourt.ai is their
manual-scoping path and is not ours to trigger.