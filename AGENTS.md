# AGENTS.md — Operational Learnings for Future Sessions

Project: EslaM-X/coreguard (CoreGuard Gate 4.1 Assurance) — lessons from live sessions that are **not** obvious from code/docs alone. Keep entries 1–3 lines each; add to the most fitting section, don't restate what's already in README/docs.

## Environment & Tooling Quirks

- **PowerShell 5.1 mojibake generator**: any JSON written via PowerShell here (freeze records, reports) risks CP1252 mojibake of em-dashes (—) because PS reads .ps1 files without BOM as ANSI. It happened **twice** (freeze 4.2.4 + 4.2.6). Fix: regenerate/repair via a Node script (`node -e` or a .cjs), never via PS — encoding hygiene test will fail otherwise. Where unavoidable, write ASCII only (no em-dash) or use a Unicode escape.
- **`.gitattributes -text` is load-bearing**: three trees (`coreguard-assurance/**`, `coreguard-assurance-v4.2.2-remediation/**`, `legacy-quarantine/**`) are `-text` (no CRLF↔LF normalization) because freeze-record hashes are byte-exact. Any new hash-verified evidence tree must be added to `.gitattributes` **before** first commit or reviewer clones won't hash-match.
- **`git show HEAD:file` ≠ disk for uncommitted work**: when verifying hash freezes, always check the **committed tree** (via `git show`), not the working disk — uncommitted edits silently make a "clean clone" verification fail (cost a full session).
- **No BOM in generated JSON**: encoding hygiene test rejects BOM in all files except `.ps1` and `scripts/verify-live.json`. Writing UTF-8 with BOM via PS (`-Encoding UTF8`) fails the test; use Node or `-Encoding UTF8NoBOM`.
- **`node --test` accepts globs/dirs**: `node --test test/encoding/` may match nothing silently. Point at the test file itself or use `npm test`.
- **Adding a `packages/*` workspace requires a lockfile regen**: declare a new package and `npm ci` fails every job (Demos/Engine/DDE) with "Missing: @coreguard/x from lock file". Red runs 35478251772 (delivery) + 35490813895/823 (pricing) were this. Guard: `scripts/check-lock-sync.mjs` (runs in CI **before** `npm ci` and in `npm test` via `test/ci/`); fix = `npm install` at root + commit `package-lock.json`.
- **Multi-line plain `run:` scalars build ZERO jobs**: a `run:` value that is a plain (unquoted, non-block) scalar continuing on the next indented line makes the whole workflow unparseable — GitHub marks the run `failure` with no jobs/logs. Red run 35480761761 (commit 6c4ca1d) died this way. Always use `run: |`. Guard: `scripts/check-workflows.mjs` (CI + `test/ci/`).
- **The zero-jobs trap is not a one-off**: it struck 6 times — 5 runs on 09-11 (`5162c10`, `19e8e8f`, `3e9a289`, `95de625`, `37c8b3a`; culprit `console.error('receiptId mismatch: '...)` inside a plain `run:`) and `6c4ca1d` (`{ port: 0 }`). Same signature every time: run `failure`, **zero jobs**, no logs. Guard must run before any step that could touch sensitive YAML; see `docs/ci-postmortem-2026-09-20.md`.
- **Missing `package-lock.json` fails opaque inside `setup-node`**: with `cache: npm`, `actions/setup-node` dies with `##[error]Dependencies lock file is not found` BEFORE any guard placed after it can run (red run 34440426922 at `41ed0b4`, bootstrap era). Rule: the lockfile guard runs **before** setup-node (Engine/Demos/DDE), and `check-lock-sync.mjs` returns `no package-lock.json in repo root` rather than a raw ENOENT (class-H regression test in `test/ci/lock-sync.test.js`).
- **Bootstrap-era red classes are closed, not erased** (register in `docs/ci-postmortem-2026-09-20.md`): Node18/20 `node --test` quoted-glob `Could not find .../*.test.js` (fixed `164c77b`); WebCrypto global absent on Node18 → `node:crypto` (`6efd636`); python-pin `verifier-c: Dec-C-9 got 3.12.3, expected ^3.14` → `setup-python` 3.14 (`8a66816`); forge `Stack too deep` → `51a5069`. GitHub has no run-delete/rewrite API — don't promise "making them green"; document cause + fix + guard instead.
- **`unzip` is not installed on this Windows dev box**: the old `convert-zip.test.js` shelled out to `unzip` for member verification — 2 tests failed locally while Linux CI kept passing, so Windows-only failures hid inside a green suite. Rule: no test may depend on an external CLI that CI has but Windows lacks; ZIP verification is now in-process (EOCD scan + central-directory count + local-header walk + CRC-32 recheck + `inflateRawSync`), so local and CI see identical bits.
- **`node --test` summary uses non-ASCII glyphs — ASCII grep filters "find nothing"**: PASS/FAIL lines start with `✔`/`✖` and the summary with `ℹ` (tests/pass/fail). `Select-String "^(ok|not ok|...)"` over the pipe returns nothing even when tests ran and failed — "(no output)" is a filter miss, not a green run. Capture `node --test <file> *> out.txt` and read the file tail for the real counts.
- **Feeding a git hook's stdin from Node on Windows races msys `sh`'s first `read`**: writing AND closing the child's stdin in the same tick (`child.stdin.end(line)`) lets the bundled `sh.exe` hit EOF before its first `read` (the hook's header delays that read), so the entire main-gate is silently skipped and the hook returns 0 as if it ran. Git itself never closes a hook's stdin, so production is safe — but a test harness can report a PASS for a `run` that never happened. Rule: write the refs line, keep the pipe open ~250 ms, then `end()` — and treat a too-fast `exit 0` from a gating test as "probably skipped", not "green".
- **The Pages-build race exists, and the median pusher is external tooling, not CI**: the Pages history alternates built/errored ("Page build failed") in 18–40s pairs. `chore(perf)` median commits (author `coreguard-trend-bot <noreply@coreguard.local`) land **15–21 s after every content push** (measured in `git log` committer dates: `f8b0320` 02:22:15Z → `87f9da7` 02:22:34Z = 19 s; `d69a0fc` 02:04:06Z → `15114d8` 02:04:23Z = 17 s; `4ef1cee` 01:40:46Z → `cf870a9` 01:41:02Z = 16 s). The DDE run started 02:04:11Z for `d69a0fc` — 5 s before the median landed, impossible for a workflow mid-`npm ci`; the identity `coreguard-trend-bot` is configured in NO checkout on this machine, appears in no scheduled task, and only `dde.yml` even names it — the pusher is push-triggered tooling outside the repo. Enforcement is now machine- and repo-level: **(1)** `scripts/pages-settle.mjs` (poll `pages/builds/latest`; exit 0=settled/1=unsettled/2=usage); **(2)** the DDE trend step wraps commit+push in it (`GH_TOKEN`, defer-on-unsettled); **(3)** the committed hook `.githooks/pre-push` (tested 5/5: abort-while-in-flight, pass-when-settled, non-main passthrough, `PAGES_SETTLE_SKIP=1` emergency bypass) gates EVERY `main` push from a checkout with `git config core.hooksPath .githooks` — install it in any other checkout (incl. the median tool's own) or have the tool run `node scripts/pages-settle.mjs` immediately before its push. Do not "fix" the alternation by coalescing medians — `perf-history.jsonl` is append-only evidence by contract.

- **A nested `node --test` silently certifies an EMPTY run**: spawned from inside a test file, node's runner sees the inherited `NODE_TEST_CONTEXT` env and prints "recursively within a test file — skipping running files" with **exit 0 and no summary** — a measurement script reads that as a green, empty result. Fix: delete `NODE_TEST_CONTEXT` from the child's env, never set it. Related: `import.meta.dirname` is Node ≥20.11; on the Node 18 leg the whole test file FAILS TO LOAD (not ok at load time) — use the repo-wide `dirname(fileURLToPath(import.meta.url))` idiom in any test. Both struck as red CI legs on 2026-09-25.

## Hidden Relationships & Chain-of-Integrity

- **Freeze-record hash → any file edit invalidates it**: editing a file in `release/freeze/` scope (including README, H1-H8.md) invalidates its SHA-256 in `freeze-record-4.2.6.json`. Rule: after any content edit, re-pin the hash inside the freeze record (keep `frozenAtUtc` unchanged), then re-verify 49/49 from disk. Re-pin `commit` refs must point at a commit **on main's history** (after any rebase, re-point the ref before pushing — a ref to a rewritten commit hashes fine locally but is `COMMIT-MISSING` on CI).
- **Deleting docs still pinned in older freeze records breaks historical verification**: `freeze-record-4.2.5.json` pins hashes of `v4.2.5-closure/engine-semantics-contract.md` and `external-evidence-model.md`. If those files are ever deleted, 4.2.5 becomes unverifiable for those entries. Any deletion must be documented in the superseding record (currently a known gap).
- **`reviews-extra/` is external evidence (Option B model)**: the assurance cycle intentionally fails (`BLOCKED exit 1`, `INV-001 fail-closed`) on a clean clone because `reviews-extra/` (in `D:\KOSSASHI\CORE DAO\reviews-extra\`) is deliberately outside the repo. Do not "fix" the BLOCKED; it's documented fail-closed behavior. A clean clone still can run SelfTest/NegativeSuite — only the standard run needs external evidence.
- **SelfTest vs standard-run scope**: SelfTest (21 checks) and NegativeSuite (16 scenarios) are environment-independent and always runnable from repo alone; the "standard run" (`-OutDir release`) rewrites frozen outputs (`selftest.tap`, `negative-tests.json`, `verification-report.json`) — don't run it casually over frozen artifacts; verify determinism by re-running into a separate OutDir and diffing.
- **Legacy quarantine**: all legacy broadcast/private-key scripts live under `legacy-quarantine/2026-09-18-v4.2.2/` and stay byte-identical to their originals (proof via `orig|quarantine` pairs in the manifest). They are outside the LEG-001 scan roots by design; do not reintroduce them into `scripts/`.
- **Manifest vs freeze-record**: `release/manifest.json` pins a subset of files (engine outputs); `release/freeze/freeze-record-*.json` pins everything (release + quarantine). The internal manifest can lag the engine version — check it against current engine outputs before trusting a "current" state claim.

## CoreGuard Governance State (binding)

- **Decision: CONDITIONAL NO-GO** — C7=PARTIAL · C8=PENDING · C9=PENDING. Signing, commitIntent, anchorProof, Mainnet broadcast are NOT AUTHORIZED regardless of any test results; GO requires separate explicit human sign-offs (C7 security, C8 independent H1–H8 signature, C9 owner decision).
- **SUPERSEDED 2026-09-19 — Owner-declared Release (see `docs/OWNER-DECISIONS-2026-09-19.md`)**: Gate 6 (independent validation) CLOSED by owner decision via the strongest machine-provable independence (3 independent re-derivations B/C/on-chain each 66–67 PASS + clean-clone determinism 749/749 ×2). **External human review and legal review were NOT performed and must NEVER be claimed** by agent or docs. Production release is OWNER-DECLARED (non-prerelease), not third-party-assessed. The CONDITIONAL-NO-GO on *signing/broadcast* remains BINDING and UNCHANGED for the agent — never sign, send, or use private keys on the agent's own initiative; the on-chain anchor already exists and is live-verified, no new broadcast is planned.
- **H1-H8.md (release/v4.2.2-remediation/) is the only sanctioned command surface**: the reviewer runs literal commands from that packet, nothing else. If engine version changes, the packet's expected outputs (e.g., "16 scenarios", "649 checks") must be updated in the same cycle — otherwise the reviewer fails a passing engine.
- **Semantics contract (engine 2.1.2)**: NOT_APPLICABLE/ABSENT never count as success for mandatory negative scenarios; PRECONDITION_FAILURE aborts the cycle with exit 1; a missing mandatory premise (e.g., `-Manifest`) must hard-fail, not silently skip. Scenario 15 was deleted as fake (its real failure mode is covered by scenario 14) — negative suite is now 16, validator 649 checks.

## Debugging Breakthroughs & Misleading Errors

**Cost-ranked index** — every entry below carries a `[C#]` tag; the rank is
its session cost, highest first: repeated red runs / burned cycles → one
session or one red run → near-miss / quickly contained. Ctrl-F a tag or a
title to jump.

| Rank | Entry | Session-cost signal |
|---|---|---|
| C1 | Multi-line plain scalar `": "` kills the whole workflow (zero jobs) | 6 documented strikes; the fake-validator trap cost a push + a red run |
| C2 | Commit-anchored verification (4.2.6+) | the byte-provenance defect class consumed several assurance cycles |
| C3 | `git show HEAD:file` ≠ disk ≠ fresh-clone disk (CRLF/autocrlf) | silent cross-machine pin drift; forced DEC-C-9 + pristine-checkout rebuilds |
| C4 | Bilingual dashboard i18n architecture | three defects killed the first complete attempt |
| C5 | i18n dictionary generation trap | mixed-content undercount → false coverage claims |
| C6 | Async render loops in review/verification UI | cost a full session (visual explainer) |
| C7 | postCommitRePins honesty discipline | a real red CI run (35398048308) |
| C8 | Freeze-record hash mismatch after PS write | mojibake misread as drift → wasted re-freeze suspicion |
| C9 | "npm test 696/696 PASS" is not a green light | mojibake surfaces there first; misdirected triage |
| C10 | Resolve pin paths before declaring a freeze violation | near-miss: almost triggered an unnecessary re-pin cycle |
| C11 | NegativeSuite premise path | contained: the suite fails correctly — just run it from the platform dir |
| C12 | Stale GREEN — session-persisted candidates mutate between verdict and use | near-miss class: defensive re-reading made it contained |
| C13 | Hand-built ZIP without unix external attrs extracts mode-0000 on Linux | real red CI run (EACCES); invisible on Windows — CI caught it |
| C14 | `process.exit()` after a big stdout report truncates its tail on Linux | real red CI run (reviewer-table contract lost the perf/verdict tail); invisible on Windows |
| C15 | `node -e` assumes CJS on Linux without a nearby package.json | contained: module-type mismatch surfaced as eval-scope error; byte-certain remedy |
| C16 | Parallel test files re-generating the same live fixture in place | flaky cross-file interference (suite passed on rerun); platform-dependent |
| C17 | System `unzip` dependency on Windows | 2 local failures invisible on Linux CI; rewrite to in-process ZIP verification |
| C18 | Pages-build race from an external median pusher (`coreguard-trend-bot`, 15–21 s after each push) | alternates built/errored in pairs; 3-layer guard closes it (workflow defer + settle script + committed pre-push hook) |
| C19 | `core.autocrlf=true` poisons every hash-pinned surface `.gitattributes` doesn't shield | Windows working-tree refresh rewrote DDE fixture records LF→CRLF; repair = `rm` then `git -c core.autocrlf=false checkout -- <file>`; probe with `git ls-files --eol`, never grep `$'\r'` |
| C20 | Node 22+/24 auto-detect ESM syntax; Node 18/20 don't — a package-less sandbox makes `.js` demos + multi-line `node -e "import …"` CJS on the Node 18 CI leg | dev box + Node 22 stayed green while Node 18 was red; pins: sandbox `examples/package.json` `{"type":"module"}` + `\s*`-tolerant ESM transform lookahead; verify with a real Node 18 binary first on PATH |

| C21 | Live-fixture destination page is a SURFACE: ship path and browser path must be the same folder; re-injection destroys bare hand-edits | silent two-place divergence while the browser boot kept working; one structural fix wiped by `git checkout` and re-applied by hand |
| C22 | Browser Buffer shim statics must return real Buffer instances | near-miss: a plain Uint8Array `concat` result resolves `toString` to radix semantics — comma-separated decimal bytes instead of text |
| C23 | Allowlist is guard 0: 403 before the rate limiter; a 200 from an unfiltered endpoint is the contract too | near-miss: a "probe → expect 403" verifier fails the honest default posture; the `/health` exemption must not be copied from the limiter |
| C24 | A factory awaited is still a factory: runPc passed ENGINE_READY's factory to callHandler — HTTP 0, silent | a dead control on the published flagship page since its launch; caught only when a deep link clicked it live |
| C25 | A documented start line is a wire claim: execute it verbatim on a live port before publishing | near-miss: the doc-curl harness proves the FIRST start per doc only — every extra copy-ready start one-liner rides CI unproven (filtered out); two shipped lines were wire-proven manually |
| C26 | The Pages API has a registration gap: /latest reports the PREVIOUS commit's terminal build right after a push, so a naive settle in that gap authorizes exactly the push that cancels the content build | 15 documented bot-race treatments; root-caused to the gap and closed by pinning every settle caller to the SHA whose build must finish |
| C27 | Identity comes from the wire, never from a header — and any verifier that can drift between two channels must be pinned to one | three cycles converged on one class: forged XFF cannot mint a peer (E2E), a settle guard measured AGE where the race was about IDENTITY, static page assertions failed on runtime-built hrefs until the source was made literal |
| C28 | A number is only authoritative if a clean clone reproduces it — and a nested `node --test` reports exit 0 with ZERO summary | a disk-walk file count certified 1016 locally vs 764 on a reviewer clone (CI red); the nested docs-node "measurement" silently certified an empty scan; both fixed (git ls-files committed-tree count + NODE_TEST_CONTEXT deleted), then the Node 18 leg failed to LOAD the test (import.meta.dirname) — third red |
| C29 | Owner mandate: pin the truth to canonical measured surfaces (snapshot + CI enforcement) before any external campaign claims a number | canonical state machine built (current-state.mjs / state-snapshot / run-tests enforcement / current-state.test); three red CI legs fixed on 2026-09-25; campaign numbers now reproducible from a fresh clone |
| C30 | A gate that reports green with no assertion behind it is worse than no gate, because it gets trusted | release-gate.mjs shipped with four self-inflicted false passes: a counter reading the wrong field (`c.pass` vs the packages' `[name, boolean]` tuples -> webhook reported `0/8` AND passed), a `module loaded` fallback that passed modules it had asked nothing, repo-relative path resolution done against the wrong root, and a banned-credential regex whose own literal matched its own source; PLUS `spawnSync("npm.cmd", shell:false)` returning a null status and no error, so five legs read `Command failed` with zero output |
- [C1] **Multi-line plain scalar with `": "` kills the whole workflow (zero jobs)**: a `run:` value written as a plain (non-block) multi-line YAML scalar containing a JS literal like `console.error('health check failed: ...')` — the `": "` inside a plain scalar is unparseable → GitHub marks the run `failure` with **zero jobs built**. That zero-jobs conclusion IS the signature of an unparseable workflow file (not a failing step). Fix: any multi-line `run:` must be a real block scalar (`run: |` on its own line). Trap within the trap: a string-matching "validator" whose `||` fallback jumped to a fake parser reported PASS — only a real YAML parser or GitHub itself building the job proves the file parses (cost a push + a red run). Now enforced mechanically: `test/ci/workflow-yaml-contract.test.js` parses every `.github/workflows` file and the composite action with the real `yaml` parser (dev dep) plus an Actions schema walk — an unparseable or schema-deviant file fails the push locally with file+line naming, before GitHub ever sees it. Runs inside `npm run prepush` too (scripts/pre-push-check.mjs), the mechanical pre-push checklist built from the YAML/workflow lessons: workflows-parse [C1] · freeze-integrity [C2] · pin-eol-cover [C3] · no-skip-ci (the skip-token vortex) · repin-reachable (the rebase rewrite trap) · remote-fresh (the bot race, with the autostash remedy named).
- [C2] **Commit-anchored verification (4.2.6+)**: `validate-freeze.mjs` hashes `git show <gitCommit>:<path>` blobs, not disk bytes. Disk mode (`--disk`) is triage-only, prints "not evidence". Records without `gitCommit` fail COMMIT-UNANCHORED (historical included — correct). Editing a file whose bytes exist only in a future commit requires a **two-phase re-anchor**: commit phase 1, then point `gitCommit`/`postCommitRePins` at that hash in phase 2. Release entries are platform-dir relative; quarantine entries repo-root relative; repo-root files outside the platform dir use `../` prefixes (stripped by `treePath`/`repoRelPath` — the honesty test must mirror this or bare-name pins hash the wrong file).
- [C3] **`git show HEAD:file` ≠ disk ≠ fresh-clone disk**: a pinned file outside every `-text` rule in `.gitattributes` is silently CRLF-rewritten on ANY fresh clone when `core.autocrlf=true` is set (system default on Windows) — the main working tree can hold LF bytes while a reviewer's pristine clone drifts and fails `--disk`. Always prove "clean-clone" claims from an actual fresh clone/`git -c core.autocrlf=false clone`, not from the dev disk. Any file newly pinned in a freeze record (including `../docs/…` — root-level pins outside the three `-text` trees) must be added to `.gitattributes` **and** the RC manifest must be rebuilt from a pristine `autocrlf=false` checkout (`scripts/build-rc-manifest.mjs` reads the disk, so generating on an autocrlf machine pins CRLF hashes that blobs don't match).
- [C4] **Bilingual dashboard i18n architecture (the pattern that works)**: three defects killed the first attempt — (1) swapping only the first text node left Arabic tails around inline children (`.ltr`, badges); (2) the resolver `L()` was scoped inside an IIFE that ran after the views' initial render → `L is not defined` + dead toggle; (3) attr swaps targeted markup markers that didn't exist. The working design: **hoisted global `L()` prelude** at script top reading live language from `window.cgLang` at call time; **full `innerHTML` swap** per `[data-i18n]` element (text + inline children together, no tails); **views re-render on a `cg41:lang` CustomEvent**; dynamic strings stored as `{ar,en}` pairs resolved through `L()`; attrs applied by unique selectors, not markers. Dictionary generated by structural pairing (637/637 nodes) against the EN sister — never hand-maintained.
- [C5] **i18n dictionary generation trap**: naive text-node pairing undercounts mixed-content elements; generate the dict by lockstep DOM-walk pairing (tag+index paths, EN `printhead` subtree dropped first) and capture full `innerHTML` per element. Verify coverage honestly: every Arabic run in the raw body must land in exactly one slot, and the EN side must contain zero Arabic chars.
- [C6] **Async render loops in review/verification UI**: the classic bug is `if (stateA !== stateB) render()` inside a promise `.then` — inverts to a permanent re-render loop. Fix: capture `target` state at render start, apply with `target` (not live state), and only re-render if live state differs from target. Cost a session to debug in the visual explainer.
- [C7] **postCommitRePins honesty discipline**: every re-pin entry must be honest vs its declared commit — the test catches duplicate/stale entries for the same file (a real CI failure, run 35398048308). Never leave superseded re-pin entries behind. The contract tests read the expected PASS count from the record itself, never hardcoded literals — a frozen `46/46` breaks on every honest pin addition.
- [C8] **Freeze-record hash mismatch after PS write**: the hash pinning mismatch looks like "file changed after freeze" — usually it's actually mojibake corruption of a field (em-dash) that changed the bytes. Check the file content for mojibake-style high-byte sequences (C3 xx / C2 xx patterns around em-dash positions) before assuming drift. Never quote a mojibake sample literally in AGENTS.md/docs — the hygiene scanner flags the example itself.
- [C9] **"npm test 696/696 PASS" is not a green light for assurance claims**: encoding-hygiene tests are inside the 696; mojibake failures appear there first. If CI is red on encoding, suspect the most recently generated JSON (written by PS) before suspecting code.
- [C10] **Resolve pin paths before declaring a freeze violation**: record paths are relative to the record's own directory — `README.md` there pins the *platform* README; the repo-root README would be `../README.md` (absent ⇒ not pinned). A session nearly triggered an unnecessary re-pin cycle because a naive `pinned.includes(changedFile)` scan matched the platform pin against a root-README edit. Normalize paths with `treePath`/`repoRelPath` semantics (or diff against the anchor with the same normalization) before starting any re-pin.
- [C11] **NegativeSuite premise path**: `-Manifest` is resolved via `Test-Path -LiteralPath` from the CWD — run the suite from the platform dir, not the repo root, or the manifest premise becomes PRECONDITION_FAILURE and fails the whole cycle (correctly). It rewrites frozen `negative-tests.json`; determinism proof = git diff empty after rerun.
- [C12] **Stale GREEN: session-persisted candidates mutate between verdict and use (read scope consent defensively)**: a candidate directory that survives across steps or sessions is a mutable surface — the consent/disclosure scope (and any record) can change between the gate's GREEN verdict and the moment a consumer (e.g. `convert-to-fixture.mjs`) reads the files, so a **remembered** GREEN from an earlier step would package records the gate never saw. Defensive reading: a verdict binds to exact bytes (pins), and every consumer re-runs the gate on the same tree it reads — never trust a verdict carried across steps. The intake pipeline does exactly this: `convert-to-fixture.mjs` re-runs the gate as its first act on the directory it then reads, and re-verifies pins against disk after write — the reason the pipeline never converts on stale consent.
- [C13] **Hand-built ZIP needs unix external attrs or Linux extracts mode-0000**: writing only the local headers/CRC leaves the central-directory external-attributes field zero; `made-by: unix (0x031e)` makes Info-ZIP derive the mode from it → every extracted file is 0000 and reads EACCES on Linux (red CI run on `1a49d16`). Windows masks this entirely — always write `(0o100644 << 16)` at offset 38 when made-by is unix. The archive's determinism tests stay green on Windows either way, so only a Linux runner catches it.
- [C14] **`process.exit()` right after a large piped stdout report truncates the tail on Linux**: stdout writes to a pipe are asynchronous — exit before the queue drains silently drops whatever is still buffered (the new reviewer-table contract on `09b4aae` lost exactly the `perf` and `verdict` lines from a ~300-line runner report; local Windows runs are synchronous and always green, so only Linux CI showed it). Rule: CLIs that print a report set `process.exitCode` and let the event loop drain — reserve `process.exit()` for usage-error exits where the (tiny) stderr message going missing is harmless. The runner now uses `process.exitCode` in both its text and JSON branches, with the drain rule commented at each site.
- [C15] **`node -e` assumes CJS when no package.json governs the eval cwd**: the eval script's module type comes from the nearest package.json to the CURRENT DIRECTORY, not to the repo — run `node -e "import …"` (or a `.mjs`-style dynamic import) from a Linux path outside the project (or an unmanaged temp dir) and Node evaluates as CJS, where a top-level `import` is a SyntaxError; Windows sessions in this repo ran from the checkout (package.json with `"type": "module"` governs) so the same snippet worked locally. Rule: in `node -e`, never rely on ambient module type — use `node --input-type=module -e` for ESM snippets or `require()` for CJS, and always pass an explicit cwd inside the repo. Determinism holds either way: the module-type decision is byte-certain from disk state, so the same snippet is either green or red on a given machine — it never flips between.
- [C16] **Parallel test files that re-generate the same live fixture in place race each other**: two suites both calling the fixture generator against the shared `examples/delivery-fixture/` tree write the same files concurrently — one suite's pin/verify can read bytes mid-rewrite of the other's, producing interference that looks like a flaky hash mismatch (this session: a delivery-suite failure near the end that vanished on rerun; the underlying write races are platform-timing-dependent). Rule: shared fixture trees are read-only for tests — a suite needing modified bytes builds them in its OWN `mkdtemp` copy (the pattern every delivery contract test already uses) and only the canonical generator owns writes to the checked-in fixture. If a rerun goes green, hunt the interference before accepting the pass: a race that hides is still a race.
- [C17] **A system `unzip` dependency made Windows red inside a green CI**: `convert-zip.test.js` used `execFileSync("unzip", …)` for member extraction + verification; the machine has no `unzip`, so 2/912 failed locally while Linux CI stayed 912/912 green — a platform gap invisible to every reviewer on CI. The rewrite (`verifyZip`) does the whole check in-process — EOCD scan, central-directory count, local-header walk, CRC-32 recompute per member, exact landing at the CD offset — and extracts through `inflateRawSync` only after structural honesty passes. No test may require an external CLI absent from a supported dev OS.
- [C18] **The Pages-build race is real; the median pusher is external push-triggered tooling**: build history alternates built/errored in ~18–40s pairs (e.g. `87f9da7` built 02:22:36Z after `f8b0320` errored 02:22:18Z). Every `chore(perf)` median commit is authored by `coreguard-trend-bot <noreply@coreguard.local>` and lands 15–21 s after a content push (committer-date-proven `git log` interleaving, several samples above); the DDE run for `d69a0fc` started 02:04:11Z yet the median `15114d8` was committed 02:04:23Z — a workflow would still be in `npm ci`, so the pusher is a local/remote process watching the branch, not a repo file (its identity is configured in no local checkout and in no scheduled task). Defense in depth: **(1)** `scripts/pages-settle.mjs` refuses a push while Pages is mid-build (or within a 15s settle window / exit 1 after max-wait; 60s API-error fallback); **(2)** the DDE trend step runs it with `GH_TOKEN` around commit+push and defers the median (warning + reset) instead of racing it; **(3)** the committed `.githooks/pre-push` (install `git config core.hooksPath .githooks`) gates EVERY push to `main` from that checkout — the only remaining hole is a pusher with its own un-hooked checkout, which must run `node scripts/pages-settle.mjs` before its push or install the same hook. Do not "fix" the pair-alternation by coalescing medians — `perf-history.jsonl` is append-only evidence by contract.

  **STATUS (superseded in part by [C26]/[C27]): CLOSED AT THE ROOT, not just treated.** The three layers above still treated symptoms — the true mechanism was the Pages API registration gap ([C26]): right after a push, `/pages/builds/latest` still reports the PREVIOUS commit's terminal build, so every naive settle (this hook included) authorized exactly the push that cancelled the content build. Since [C26], every settle caller pins `PAGES_SETTLE_SHA` — this hook passes the NEW main tip from the ref line, and the dde.yml trend step pins the content commit (HEAD at the settle call site; the [C27] caution records the `HEAD~1` mis-pin and its two-cycle regression) — so the settle completes only when THAT commit's own build registers and finishes, and the autostash remedies in this entry remain only as belt-and-suspenders. The historical "bot race" pattern of paired cancelled pages builds should no longer appear; if it does, suspect a settle caller that lost its SHA pin or a new un-hooked pusher, and re-read [C26]/[C27] before touching this entry.
- [C19] **`core.autocrlf=true` poisons every hash-pinned surface that `.gitattributes` does not explicitly shield**: a Windows working-tree refresh rewrote the DDE fixture records LF→CRLF (index stayed LF), so `verify-fixture.mjs` failed fail-closed on its `hashes.json` pins — locally only, Linux CI green: exactly the verifier-c failure mode of `docs/eol-policy.md` (addendum 2b). The checkout trap compounds it: `git checkout -f -- <file>` refuses to rewrite a file whose clean-filtered content equals the index (git calls it clean), so the repair is `rm` the file THEN `git -c core.autocrlf=false checkout -- <file>`. Probe EOL drift with `git ls-files --eol` (`i/lf` + `w/crlf` on a pinned file = poison), never with `grep -c $'\r'` (shell/tool quoting lies). Rule: every byte-exact surface needs an explicit `-text` rule plus a checkout-index byte-parity proof under simulated autocrlf (`test/eol/eol-policy.test.mjs`); the generator's LF-only writes remain the single legitimate write path.
- [C20] **Node 22+/24 auto-detect ESM syntax in `.js` files and `node -e`; Node 18/20 do not**: the docs-node contract ran a copied `.js` demo (`examples/transfer/run-demo.js`) from a package-less sandbox tmp — Node 22+ ran it as ESM, Node 18 read it as CJS and died on `import`, and the same split hit multi-line `node -e "import …"` stanzas whose JS starts on the absorbed next physical line. The dev box (Node 24) and Node 22 legs stayed green while the Node 18 leg was red — reproduce by running `npm test` with a real Node 18/20 binary first on PATH. Fixes: (1) the sandbox tmp now carries its own `examples/package.json` `{"type":"module"}` (file-scoped — the cwd-based CJS `node -e "const fs = require(…)"` stanzas at tmp root still resolve as CJS), and (2) the docs-node contract's [C15] transform lookahead tolerates leading `\s*` between `node -e "` and the `(`/`import ` marker so absorbed multi-line ESM blocks get `--input-type=module`.

- [C21] **The live-fixture destination page is a surface, not just an output — ship path and browser path must be the same folder**: the injected demo pages boot their embedded engine in the browser from `packages/delivery/` via an import-map; authoring/previewing a copy from a different folder worked yet silently diverged from what the repo ships (the same fix would have to live in two places, and only one of them is reviewed). Rule: inject into and boot from the canonical `docs/` path only. Two derived traps: (1) **re-injection rewrites the page — a bare hand-edit (the injector writes records + shims + payload, never script content) is destroyed by regenerating or by `git checkout`; restoring a page from git means re-run `inject-demo-data.mjs` AND re-apply the hand-fix** (this cycle: a one-line bare `@coreguard/canonical` mapping fix was wiped once by checkout and re-applied manually); (2) the injector now carries an inject-time mapping check that fails the injection when the bare mapping is missing or mis-ordered, and `test/delivery/sdk-ref-page.test.js` boots the shipped page browser-equivalently outside the repo, so the divergence class is caught mechanically.
- [C22] **A browser Buffer shim's static methods must return real Buffer instances, not plain Uint8Arrays**: the `node:http` shim exists only so `Buffer.concat(chunks).toString("utf8")` drains request bodies in-page. The near-miss: `static concat` returning a plain `Uint8Array` passes `instanceof Uint8Array` and `.set()` — until `.toString()` resolves to `Uint8Array.prototype.toString`'s **radix** semantics and emits comma-separated decimal byte lists instead of text. Same bug class for `Buffer.from`. Fix: `class Buffer extends Uint8Array` whose statics return `new this(...)`; `toString(enc)` overrides via TextDecoder (utf8 is the only handler surface; anything else throws), `from` covers string(utf8)→bytes (the artifactSha256 surface) and copy-from-Uint8Array. Locked by the browser-boot contract tests (`test/delivery/api-ref-page.test.js`, `test/delivery/sdk-ref-page.test.js`) that run the shims outside the repo on every push.
- [C23] **The allowlist is guard 0 — 403 must fire before the rate limiter, and a 200 from an unfiltered endpoint is the OTHER documented posture**: the address gate is checked before anything else, so a rejected peer never consumes rate-limit budget and never reaches `limiter.check` — a flood of disallowed addresses cannot grow the buckets map (the limiter's memory bound covers admitted peers only). The exemption split is deliberate and easy to "fix" wrongly: the limiter exempts `/health` (floods must never lock out liveness probes) but the allowlist does NOT — an unlisted address learns nothing, not even liveness; the two rules protect against different failures (lockout vs enumeration), so exempting `/health` from the allowlist "for consistency" would let any unlisted address confirm the service exists. Near-miss when probing the guard externally: a verifier shaped "fire the probe → expect 403" fails the honest DEFAULT endpoint — with no `allowAddresses` configured the gate is open and `/health` answers `200 OK`; **the absence of 403 is itself the documented contract, not a failure**. Proof shape that actually works: start a second endpoint pinned to a FOREIGN allowlist (`allowAddresses: ["198.51.100.9"]`) and probe it from an unlisted loopback address (the api-ref page's defense-code wire contract does exactly this), while the honest suites keep proving the unfiltered 200 posture; the page's 403 button spells out both postures so a visitor can never read a 200 as a bug.
- [C24] **A factory awaited is still a factory — passing ENGINE_READY itself to callHandler answers HTTP 0, silently**: the boot prelude exposes ENGINE_READY as a FACTORY (an IIFE resolving to `() => mod.createDeliveryRequestHandler()`) so every scenario gets a fresh handler — fresh rate-limit budget, exactly like a fresh server process. The six table scenarios do it right (`const makeHandler = await ENGINE_READY; … callHandler(makeHandler(), …)`), but the peoples-court button did `const handler = await ENGINE_READY` and handed the factory itself to `callHandler`: invoking the factory "worked" (it returned a handler that was never actually driven against req/res), `res.statusCode` never left 0, no exception was thrown, and the catch block had nothing to catch — the visitor saw "HTTP 0" over an empty body. The tell is the signature: **statusCode 0 = the handler never ran** (distinct from every real wire code); anything handed to callHandler must already be `(factory)()`. Why it survived publication: the boot sequence exercises only the six SCENARIOS — runPc is never clicked during boot, the page contract was static-only, and nothing crashed. Locks now: the api-ref page contract asserts the fresh-handler shape at the /peoples-court call site and fails the misfire literal outright — and the general rule stands: a control only a human click reaches is untested until something clicks it (the `#run=` deep links now do).
- [C25] **A documented start line is a wire claim — execute it verbatim on a live port before publishing**: a copy-ready `startDeliveryEndpoint` one-liner in a doc makes a falsifiable claim about real wire behavior; "it obviously works" is how a broken flag order or a port typo ships to every integrator. The doc-curl harness proves only the FIRST start line per document (it needs one live server and re-uses it) and explicitly FILTERS every further start command out of the fences it executes — so each additional copy-ready start line rides CI unproven, invisible to every reviewer. The discipline that closed it: before each of the two new documented lines shipped (the §integration `allowAddresses` line and the §production proxy-posture line), the exact string was executed on an ephemeral live port and its claimed observable was asserted — `200 VERIFIED` with the pinned dual-stack loopback answering, and `x-ratelimit-limit: 600` proving the backstop sizing took. Rule: every documented start one-liner is either (a) executed by the doc-curl contract as THE documented start, or (b) wire-proven verbatim in-session before the commit — and if a third line ever needs regular proving, that is the signal to extend the harness to start-per-line instead of trusting session discipline.
- [C26] **The Pages API has a registration gap — pin every settle to the SHA whose build must finish**: timing evidence (DDE Evidence Cycle 14–21 s; bot commits ~15–20 s after content pushes) showed the trend step's `pages-settle` check ran while the CONTENT commit's build was not yet registered in `/pages/builds/latest` — which still reported the PREVIOUS commit's terminal, aged build — so the guard authorized exactly the push that CANCELLED the content build. That is the recurring "bot race" (15+ autostash treatments; [C18]'s three layers never addressed the gap itself). The fix: `PAGES_SETTLE_SHA=<sha>` pins the settle — the guard settles ONLY when the latest build IS that commit's and is terminal+aged. State logic: (1) build absent + SHA on the remote branch (compare `behind`/`identical`) → registration gap → WAIT; (2) build absent + SHA NOT on the remote → pre-push semantics (the push has not landed; nothing alive to cancel) → settle; (3) latest build is a DIFFERENT commit → same two-way probe (pre-push settles immediately; bot side waits). Probe failure degrades conservatively to wait. Callers: `.githooks/pre-push` passes the NEW main tip from the ref line (all-zero/empty ⇒ unpinned legacy behavior — deletion is nothing to settle); the dde.yml trend step pins the CONTENT commit and defers the median without an autostash when the content build cannot settle. CAUTION learned in production: at the settle call site HEAD is STILL the content commit (the median commit is created only after settle succeeds) — pinning `HEAD~1` there silently re-opened the race for two cycles (the wrong SHA answered "not on branch" and short-circuited the guard) until the pages-build list exposed the returning cancelled pattern; verify the pinned identity from the actual ref state, never from an assumption about what HEAD means at that point in the script. Lesson twice over: a timing-derived "age" guard proves only that SOME build finished — identity, not age, is what a cancel races on; and the first settle design conflated the two sides of the wire (pre-push vs bot) until a test froze each side's semantics separately.
- [C27] **Identity comes from the wire, never from a header — and any verifier that can drift between two channels must be pinned to one**: three separate cycles this period converged on a single failure class, which is the trust architecture investors and integrators are actually buying. (1) **Wire identity**: the E2E proxy topology proved a peer's identity is its SOCKET address — a stranger can forge `X-Forwarded-For: ::1` verbatim (even claiming the allowlisted proxy's own identity) and still gets 403, because allowlist and rate-limit decisions key on the socket, and forwarded headers are LOGGING data, never identity. (2) **Verifier drift**: a settle guard that measured AGE ("some build finished") authorized a cancel that races on IDENTITY ("that commit's build") — fixed by pinning every settle caller to the SHA whose build must finish. Same class in-page: the cross-link contracts statically assert literal hrefs, but the page BUILT them at runtime from a template variable — verifier and source of truth lived on different channels, so the assertion failed until the page source itself was made literal (fix the artifact, never loosen the check). (3) **Why this is the product**: CoreGuard's whole pitch to a funder or an integrating company is that every claim is mechanically checkable and every check is drift-proof — evidence pinned by hash (C2), identity keyed on the wire (this entry), verdicts bound to exact bytes (C12), and contracts that fail the push when prose and code diverge. The general rule: whenever a verifier and the thing it verifies can disagree (two channels, two moments, two transports), pin them to ONE channel and make the artifact carry the proof — and when tempted to relax an assertion to match reality, treat that as the signal that the ARTIFACT is lying, not the test.
- [C28] **Democracy-for-numbers: a count is only authoritative when a clean clone reproduces it, and a measurement read as green can be empty**: this session built the canonical measured-state machinery after the owner mandated real numbers before any campaign. Two traps surfaced. **Trap 1 (disk vs committed tree)**: the boundary audit counted files on the DEV disk — my checkout carried ~252 local untracked/ignored files, so it certified 1016 files while a reviewer clone sees 764; every number claimed from disk writes is not reproducible on another machine. Fix: `scripts/current-state.mjs` reports `filesScanned` from `git ls-files` (same exclusions as the audit) — the count a fresh clone reproduces — while the audit still scans disk for content checks. **Trap 2 (nested `node --test`)**: measuring the docs-node contract by spawning `node --test` from inside a test file made the runner print "recursively within a test file — skipping running files" and exit **0 with no summary**; a check that only looks at exit code reads that as a green, empty measurement (the machine-verification version of a filter miss [AGENTS tooling quirk]). Fix: `delete process.env.NODE_TEST_CONTEXT` in the child env. **Trap 3 (Node 18)**: `import.meta.dirname` is Node ≥20.11; on the 18 leg the whole test file failed to LOAD (not ok at load, not a normal failure) — two red engine legs before the repo idiom `dirname(fileURLToPath(import.meta.url))` was used. The resulting contract: `scripts/run-tests.mjs` enforces the suite pass total against `docs/state-snapshot.json.testsTotal` after every green run (a stale number in any doc = failing build); `test/ci/current-state.test.js` re-measures the fast fields and fails on drift; `gitCommit` in the snapshot is an ANCESTOR pin (rebase-proof), never `== HEAD` — content fields are what CI compares.
- [C29] **Owner mandate: pin the truth to canonical surfaces before any external campaign**: the owner's directive (binding) is that every number a letter or doc states must be machine-measured, reproducible from a fresh clone, and CI-enforced — before Phase A/B outreach material is shared. The People's Court engagement remains an external TECHNICAL SIGNAL (never endorsement/partnership/donation); nothing external is sent without a per-step owner sign-off (P7 §5/§6); results beyond verification (funding, trends, wealth) must never be promised. Campaign language must use only the claimable-facts instrument lines (`docs/owner-decisions-and-claimable-facts-2026-09.md`).
- [C30] **A gate that can report green with no assertion behind it is worse than no gate, because it gets trusted**: the release gate shipped with four false passes inside itself, all found only by running it. **(a) A counter reading the wrong field name**: the self-check cases come in two shapes in this tree — objects with `pass`/`ok`/`valid`, and the packages' `[name, boolean]` tuples — and the gate hard-coded `c.pass`, so webhook printed `0/8 invariants` **and returned PASS**. `tallyCases()` now reads both shapes explicitly and an unreadable case is a FAILURE, never a pass; callers must check the denominator, because `0/0` reads exactly like `8/8` if you never look. **(b) A `module loaded` fallback**: a module exposing no self-check was waved through as PASS — a green light with no bulb behind it. It now FAILs, and pure libraries are proven by naming the committed test files that *import* them (a named file that does not import the module is the same empty pass). **(c) Repo-relative paths resolved against the wrong root**: `join(REPO, "scripts", leg.module)` made `packages/*` legs audit `scripts/packages/*` and sent every dynamic `import()` to a path that did not exist — import by `pathToFileURL(join(REPO, leg.module))` and store module paths repo-relative. **(d) The audit matched its own source**: a banned-credential pattern written as a contiguous literal matches the line that defines it, so a self-auditing file fails itself forever; assemble such patterns from fragments. **Plus the silent spawn trap (same family as the zero-jobs signature)**: `spawnSync("npm.cmd", …, { shell: false })` on modern Node returns `status: null` with NO `error` and no output, so five heavy legs reported `Command failed` with nothing to read — a failure with no cause is indistinguishable from a failure you did not look at. `npm.cmd` is a batch shim; resolve npm's real JS entry (`process.env.npm_execpath`, else the platform `npm-cli.js`) and spawn it under `process.execPath` with no shell, and always print `out.error`. Locked by tests in `test/integrations/developer-kit.test.mjs` (tally shapes, denominator non-zero, library legs import-named, child legs map to real npm scripts). The generalization: **any verifier that reports a count must fail on a zero denominator, and any self-auditing check must be run at least once against itself before it is trusted with anything.**

## Files That Change Together

- `H1-H8.md` expected numbers ↔ engine version ↔ `npm test` test counts ↔ validator check count (currently 2.1.2 ↔ 16 ↔ 1153 ↔ 649 — the npm test number tracks the latest measured count).
- `freeze-record-4.2.6.json` ↔ any file it pins ↔ `.gitattributes` tree list ↔ README pin-count claim (now 47 = 28+19 after the dashboard joined; README.md's own pin is inside the record it links from). The dashboard's `#payout` section is bilingual per the AGENTS.md i18n pattern: its `test/p2/governance-dashboard-i18n.test.js` contract keeps the EN markup defaults and the AR dictionary structurally paired — edit one side only and the pairing check fails by name.
- Bilingual dashboard pair ↔ AGENTS.md i18n pattern above ↔ the EN mirror (`coreguard-gate41-visual-en.html`) — regenerate the dictionary by structural pairing after ANY structural edit to either file; hand-edits to one side silently break the 637/637 node pairing.
- `verification/cg-assurance-verify.ps1` self-test count (21) ↔ `selftest.tap` frozen bytes ↔ README/STATUS claims (21/21).
- Commercial surfaces ⇄ snapshot ⇄ `docs/verification-and-integration-platform-2026-09-26.md` §12 ⇄ `docs/commercial-packaging-2026-09-26.md` ⇄ `docs/conformance-report.json` ⇄ `docs/adoption-dashboard.md`: `packages/webhook` (CG-WH/1 receiver) + `packages/x402` (X402/1 gated harness) + `examples/platform-quickstart` + `scripts/ladder/conformance.mjs --write` (CG-CR/1) + `test/integrations/platform-surfaces.test.mjs`. The x402 gate and the CG-CR/1 report both sit in the same push as the snapshot: the report's `badgeIssuerCount` is 0 and the dashboard's revenue is $0, and a price change or an issued badge MUST land with the number, never before it. The packaging doc is machine-checked for cited paths and for any `$` amount other than $0.

- A–J honesty bundle ⇄ snapshot ⇄ the control plane ⇄ every citing doc: `scripts/ladder/claim-registry.mjs` (CG-CL/1) → `docs/claims-registry.json` · `packages/badge/index.mjs` (CG-BDG/1) → `docs/badge-registry.json` + `docs/badges/*.svg` · `scripts/partner-journey.mjs` (CG-PJ/1) · `scripts/integration-replay-lab.mjs` (CG-IR/1) · `scripts/release-gate.mjs` + `scripts/release-gate-child.mjs` (CG-RG/1) → `docs/release-gate.json` · `test/integrations/developer-kit.test.mjs` (+58) · `docs/badge-system-2026-09-26.md` (Arabic-first + EN mirror) ⇄ `scripts/ladder/adoption-dashboard.mjs --write` (the board now reads claims + badges + gate as committed sources) ⇄ `docs/owner-decisions…` §9. Rules that will bite otherwise: (1) `claim-registry --write` embeds its own `audit` block and exits non-zero on violations — the audit verdict must travel WITH the artifact, because the control plane reads committed files and may not re-derive a result and present it as recorded; (2) the same argument forbids the dashboard computing a number from a module: it reads `docs/claims-registry.json`, `docs/badge-registry.json`, `docs/release-gate.json` or prints `n/a`; (3) `docs/release-gate.json` is timestamp- and `ms`-free so re-running the gate produces NO diff (verified) — an artifact that churns trains reviewers to ignore its diffs; (4) `verifyBadge()` normalizes trailing whitespace, so the file is `svg + "\n"` and a final newline is not forgery, but a changed byte inside the artwork is; (5) a bulk number sync must never touch a changelog row — the `1095 → 1148` replace also rewrote the v0.5.20 historical row, which would have made a past release report a test count it never measured.


- `docs/state-snapshot.json` ↔ `scripts/current-state.mjs` ↔ `scripts/run-tests.mjs` (suite-total enforcement vs the snapshot's `testsTotal`) ↔ `test/ci/current-state.test.js` (re-measures, fails on drift) ↔ every surface citing a count — `docs/STATUS.md` test-suite + v0.5.x rows, `docs/owner-decisions-and-claimable-facts-2026-09.md` §1, `docs/current-state-audit-2026-09-25.md`, `docs/master-launch-adoption-plan-v2-2026-09-25.md`, Phase A/B docs. A count change (e.g. adding any doc file bumps `filesScanned`) must regenerate the snapshot — `node scripts\current-state.mjs --write --tests <pass>` **after** `git add` so `git ls-files` sees the new files — and refresh every citing surface in the SAME push; otherwise `npm test` goes red by design (last sync: testsTotal 1022 → filesScanned 768 → docsNode 70/53/18 → commitsAheadOfV060 76). On 2026-09-25 the ladder bundle pushed them to (last sync: testsTotal 1033 → filesScanned 780 → docsNode 70/53/18 → commitsAheadOfV060 77) — the `scripts/ladder/` modules + `test/integrations/ladder.test.mjs` + generated `docs/integration-registry.json` + `docs/adoption-dashboard.md` + `docs/adoption-milestones.json` must be regenerated (`npm run ladder:passport:write` + `npm run ladder:dashboard:write`) any time the snapshot moves, and the empty milestones array is load-bearing (external counters stay 0 until a real milestone is appended). On 2026-09-25 the v0.3 reputation/risk-findings build pushed them further (last sync: testsTotal 1043 → filesScanned 785 → docsNode 70/53/18 → commitsAheadOfV060 83, measuredAtUtc 2026-09-25T17:15:28Z) — the two new modules (`scripts/ladder/risk-findings.mjs` CG-RF/1 + `scripts/ladder/reputation.mjs` CG-RP/1) + `test/integrations/ladder-v03.test.mjs` (+10 tests) + generated `docs/risk-findings.json` + `docs/reputation-registry.json` must also be regenerated (`npm run ladder:findings:write` + `npm run ladder:reputation:write`) any time the snapshot moves; the findings JSON derives statuses only (never numbers), so RF-006/007/008 stay OK across number changes; risk findings are findings only, never a fuzzy safety score, and RF-003 goes OPEN (settlement) the moment any adapter is live (fail-closed). On 2026-09-26 the Verification & Integration Platform build moved them again (last sync: testsTotal 1061 → filesScanned 796 → docsNode 70/53/18 → commitsAheadOfV060 91, measuredAtUtc 2026-09-26T04:05:30Z) — the platform bundle (`scripts/ladder/verification-levels.mjs` + `scripts/ladder/integration-states.mjs` + `packages/adapters/contract.mjs` + `packages/adapters/providers.mjs` + `scripts/partner-sandbox.mjs` + `scripts/attack-lab.mjs` + `scripts/observability.mjs` + `scripts/passport-v2.mjs` + `test/integrations/verification-platform.test.mjs` + generated `docs/evidence-passport-v2.json`) must be regenerated with it (`npm run passport:v2:write`; the ladder registries stay with `ladder:*:write`). The L1 chain rule is load-bearing: `verifyReceipt(receipt, commitment, chain)` returns `UNKNOWN/NO_CHAIN_EVIDENCE` when no `chain.getReceipt` is passed, so ANY caller (sandbox, attack lab, future tests) that expects `VERIFIED` MUST pass a chain authority —  The commercial-surfaces build moved the pins again (last sync: testsTotal 1061 → filesScanned 803 → docsNode 81/53/18): the +7 files are `packages/webhook/index.mjs` + `packages/x402/index.mjs` + `examples/platform-quickstart/{quickstart.mjs,README.md}` + `docs/commercial-packaging-2026-09-26.md` + `docs/conformance-report.json` + `test/integrations/platform-surfaces.test.mjs`, and the docsNode jump 70→81 is the four new README commands the contract now EXECUTES (`webhook`, `x402`, `conformance:report`, `quickstart`) — which forced a real fix: the docs sandbox stages example trees by an explicit list, so a new documented example must be added to `test/ci/docs-node-contract.test.js` `buildSandbox()` or its command fails with MODULE_NOT_FOUND. an attack lab that verifies receipts structurally would let a swapped txHash pass (it did, before the rule existed, and the tamper suite caught it). The same day a THIRD honesty defect of that class surfaced: two roadmap rows ("webhook AVAILABLE" in the control plane, and the X402 provider row pointing at `packages/x402`) were PROSE ONLY — the surfaces did not exist. Rule: a surface named in an inventory (registry row, dashboard row, adapter `surface:` field, doc claim) must exist as a module, or the row is downgraded to its honest default; never leave a claim that only a sentence supports. The fix here was to BUILD the surface — CG-WH/1 (HMAC + replay window + idempotency) and X402/1 (gated on an owner-locked price) — not to reword the claim, and `test/integrations/platform-surfaces.test.mjs` now fails the build if the packaging doc cites a path that does not exist. Second, these two packages deliberately ship WITHOUT a `package.json`: `workspaces: ["packages/*"]` means any package.json there needs `npm install` + a committed `package-lock.json` or every `npm ci` leg dies at setup-node (the lock-sync trap above), and a standalone surface with no dependents only needs relative imports. Fourth, a GENERATED artifact is not a doc: bulk number-sync regexes that read like `filesScanned"?:\s*(\d+)` are safe on markdown and destructive on generated JSON, because the optional quote collapses and the replacement drops the closing quote — one pass turned `"filesScanned": 796` into the invalid `"filesScanned: 803` in `docs/integration-registry.json` and took the whole suite red with "Bad control character in string literal". Rule: never bulk-rewrite a GENERATED file; regenerate it from its module (`ladder:dashboard:write`, `passport:v2:write`, `current-state --write`, `conformance:report`) and hand-edit only the hand-written docs, then prove every touched `.json` still parses before believing the rest of the run. Third, an artifact that embeds a timestamp churns on every run and trains reviewers to ignore its diffs — `docs/conformance-report.json` carries none, and a test asserts it stays that way.
- `docs/master-launch-adoption-plan-v2-2026-09-25.md` ↔ `docs/phase-b-court-neutral-expansion-2026-09-25.md` ↔ `docs/phase-a-people-court-followup-draft-2026-09-25.md` ↔ `docs/campaign-ops-outreach-kit-2026-09-25.md` (letters, KPI, ledger, versioning policy) ↔ `docs/owner-decisions-and-claimable-facts-2026-09.md` §7 ↔ AGENTS.md [C28]/[C29] ↔ outreach truth ledger — the court-neutral campaign bundle; the lead numbers they all cite must equal the snapshot above. The Phase A draft doubled as the POSTED message on 2026-09-25T15:45:07Z (`…18601897`, verified live; a prior record claiming `…18600613` was a phantom — the comment never existed on the thread, so any record of it must be corrected to the real id) — its 1033/780 figures are the post-time historical facts; keep the body verbatim, cite the new numbers from the snapshot only in NEW claims.
- Live Integration & Adoption Ladder bundle ↔ the snapshot ↔ `docs/live-integration-and-adoption-ladder-2026-09-25.md` ↔ decisions doc §4/§7-8 ↔ B-letter track states: `scripts/ladder/` (states · state-machine · adapters · conformance · evidence-passport · adoption-dashboard · risk-findings · reputation · verification-levels · integration-states · cli) + `test/integrations/ladder.test.mjs` + `test/integrations/ladder-v03.test.mjs` + `test/integrations/verification-platform.test.mjs` + generated `docs/integration-registry.json` + `docs/adoption-dashboard.md` + `docs/risk-findings.json` + `docs/reputation-registry.json` + `docs/evidence-passport-v2.json` + append-only `docs/adoption-milestones.json`. The dashboard reads all five committed sources (snapshot · registry · milestones · risk-findings · reputation) and shows a governance block (CG-RF/1 · CG-RP/1). Any live-mode claim must come from THIS machine (state ∈ 13-state space, transitions guard-checked, conformance CG-CS/1, platform level = `ladder:status`), never from prose; the registry's three adapters are structurally NOT_BUILT/NOT_PERFORMED until a named integrator + per-step owner approval exist (Decision A/B, 2026-09-25); the reputation score equals recorded milestone entries only (1 point per entry, never the amount) and every external counter is 0 until a real milestone is appended.

## User Preferences & Decisions (binding)

- **Never sign, broadcast, or authorize Mainnet** — any instruction implying that must be refused or escalated; the owner's explicit separate decision is the only path.
- **Don't rewrite historical freeze records** — supersede with a new record (e.g., 4.2.5 → 4.2.6) preserving `frozenAtUtc`; re-pinning a hash inside a record is acceptable only for a documented exception (e.g., README mojibake repair) with the timestamp unchanged.
- **Evidence over narrative**: every "PASS" claim must be reproducible from the committed tree (not the working disk) — verify via `git show`/`git archive` before asserting.
- **PowerShell for scanning/engine, Node for JSON generation** — this split is deliberate; don't generate JSON artifacts with PowerShell 5.1.
- **Sync discipline**: local main should never lag origin/main (user's explicit standing directive) — push and verify CI before declaring a cycle closed.
- **Arabic-first, English-mirrored**: all visual explanations and dashboards are authored in Arabic (the user's native language) as the source of truth, then mirrored to English at world-class documentation-brand quality. Every future visual-explanation request expects the EN mirror; keep both files in sync fact-for-fact (same numbers, hashes, structure — LTR/RTL only differs).
